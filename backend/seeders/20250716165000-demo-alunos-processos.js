module.exports = {
  up: async (queryInterface, Sequelize) => {
    // Adicione aqui os dados de seed se necessário
    return Promise.resolve();
  },
  down: async (queryInterface, Sequelize) => {
    // Remova aqui os dados de seed se necessário
    return Promise.resolve();
  }
};
